# Forensic Search System - Dependencies
# =====================================

# Core Gemini SDK
google-generativeai>=0.8.0

# Async HTTP
aiohttp>=3.9.0
httpx>=0.25.0

# Environment management
python-dotenv>=1.0.0

# Data validation (optional but recommended)
pydantic>=2.0.0

# Note: The rule-based query builder works without any external dependencies
# Only the AI-powered investigation requires google-generativeai